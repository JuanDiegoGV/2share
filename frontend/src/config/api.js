const url = 'http://localhost:3000/api/'

const endpoints = {
  session: url + 'session',
  user: url + 'user',
  userProducts: url + 'userProducts',
  products: url + 'products'
}

export default endpoints
